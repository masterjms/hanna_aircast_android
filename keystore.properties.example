# 이 파일을 keystore.properties 로 복사한 뒤 값을 채운다.
# keystore.properties 와 .jks 파일은 절대 커밋하지 않는다(.gitignore 에 있다).
#
# storeFile 은 저장소 밖의 절대 경로를 권한다. 역슬래시 대신 슬래시를 쓴다.
storeFile=C:/Users/<이름>/keys/hanna-wifisetup.jks
storePassword=
keyAlias=wifisetup
keyPassword=
